{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock title %}

{% block body %}
    <H2 class="text-light text-center m-5">Avis</H2>
    {%for avi in avis %}
    <div class="register m-5 p-2 detail">
        <div class="text-light m-2 ms-5">
            <img class="rounded-circle bg-success" src="{{ path('user_photo', { id: avi.userId.id }) }}" alt="image utilisateur"  width="100" height="100">
            {{avi.createdBy.getEmail()}}
        </div>
        <div class="d-flex">
            <div class="text-light m-2 ms-5">
                <img src="/media/star-fill.svg" alt="star avis">
                {{avi.note}}
            </div> 
            <div class="text-dark rounded opacity-50 bg-light m-2 p-2 ms-5">
                <p>{{avi.commentaire}}</p>
            </div>

        </div>
    </div>
    {% endfor %}
    
{% endblock %}
